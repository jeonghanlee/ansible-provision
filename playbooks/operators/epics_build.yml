---
# playbooks/operators/epics_build.yml
# One operator, one playbook. Species assemblies import these in the
# operator definition's order; runs are scoped with --limit.
- name: Apply the epics_build operator
  hosts: vacua
  gather_facts: false
  become: yes
  roles:
    - role: epics_build
      tags: [epics_build]
