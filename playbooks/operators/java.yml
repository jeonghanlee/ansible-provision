---
# playbooks/operators/java.yml
# Apply after common; runs are scoped with --limit.
- name: Apply the java operator
  hosts: vacua
  gather_facts: false
  become: yes
  roles:
    - role: java
      tags: [java]
