---
# playbooks/operators/ethercat.yml
# One operator, one playbook. Species assemblies import these in the
# operator definition's order; runs are scoped with --limit.
- name: Apply the ethercat operator
  hosts: vacua
  gather_facts: false
  become: yes
  roles:
    - role: ethercat
      tags: [ethercat]
