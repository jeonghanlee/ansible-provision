---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Install base packages (Debian)
  ansible.builtin.apt:
    name: "{{ pkg_base + ['libssl-dev', 'chrony'] }}"
    state: present
