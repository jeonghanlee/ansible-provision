---
# roles/epics_build/defaults/main.yml
# EPICS-env source repository (not the -distribution binary).
repo_epics_source: "https://github.com/jeonghanlee/EPICS-env"

# Release tag to build - or a branch ref before its tag is cut.
epics_env_tag: "release-1.3.0"

# Where the source tree is cloned and built.
path_epics_source_src: "/opt/epics-env-src"

# Vendor dependency repositories built before EPICS-env.
repo_pkg_automation: "https://github.com/jeonghanlee/pkg_automation"
repo_uldaq_env:      "https://github.com/jeonghanlee/uldaq-env"
repo_open62541_env:  "https://github.com/jeonghanlee/open62541-env"

# Python runtime provisioned before the build.
pkg_python_debian:
  - python3
  - python3-pip
  - python3-serial
  - python-is-python3
  - python3-numpy
  - python3-pyparsing
  - python3-build
  - python3-nose2
  - gcovr
  - flawfinder
# Generic RedHat-family python; rocky8 overrides to the 3.9 module in its
# vacuum group_vars.
pkg_python_redhat:
  - python3
  - python3-pip
pkg_python_system:
  - python3-pyserial
pip_bin:                 "/usr/bin/pip3"
pip_packages_global:     ["numpy", "pyparsing"]
pip_packages_epics:      ["nose2"]
runtime_python_alt_name: "python"
runtime_python_alt_path: "/usr/bin/python3"
