Session ID: rs20260604_120138
Session Root: `/data/gitsrc/ansible-provision/review_sessions/20260604_120138_design_milestones`
Artifact ID: conv20260604_133009
Artifact Type: convergence_report
Acting As Role: Facilitator
Author Agent ID: codex_gpt5
Author Transport: codex
Author Model: GPT-5
Target Artifact ID: source20260604_120138
Supersedes Artifact ID: conv20260604_120635

## Role Assertion

- Agent: codex_gpt5
- Acting As: Facilitator
- Role Source: `README.md` row for codex_gpt5 and user-provided external review on 2026-06-04
- Artifact Type Allowed: yes; Facilitator may publish convergence_report per role-permissions.md.
- Target Path Allowed: `convergence/conv20260604_133009_codex_gpt5_supersedes_conv20260604_120635.md` is within this role's write scope.
- Re-Anchor Trigger: user-provided external review at turn boundary

# Convergence Report: Design Milestones

This artifact supersedes `conv20260604_120635` after external review
identified an unrecorded site-identifier leak in the NFS simulation
defaults and architecture text.

## Inputs

| Artifact ID | File | Role |
| --- | --- | --- |
| source20260604_120138 | `00_source_design_milestones.md` | source |
| rev20260604_120138 | `reviews/rev20260604_120138_codex_gpt5_initial.md` | Reviewer |
| rev20260604_120406 | `reviews/rev20260604_120406_codex_gpt5_reviewer2_public_baseline.md` | Reviewer 2 |
| rev20260604_120420 | `reviews/rev20260604_120420_codex_gpt5_reviewer3.md` | Reviewer 3 |
| external_review_chat_20260604 | user-provided chat review from external Reviewer | External Reviewer |

## Accepted Findings

| ID | State | Finding | Source Crosswalk | Decision |
| --- | --- | --- | --- | --- |
| F001 | accepted | The repository must be reframed as a public Linux system unification baseline with EPICS and IOC validation, not as a cloud-provision-only application deployer. | C1, R2-F1 | DM1 is the first documentation and architecture gate. |
| F002 | accepted | VM lifecycle ownership is the first design decision because this repository and `ansible-alsu-linux-os` show competing historical models. | C2, R2-F2, R3-F1 | Keep VM lifecycle delegated to `cloud-provision` for the first reorientation; document `inventory/testbed.ini` as an example validation inventory, not repository identity. |
| F003 | accepted | Public defaults, validation defaults, testbed defaults, and site-overlay-required values must be separated conceptually before broader restructuring. | R2-F3 | DM4 must classify variables by responsibility and overlay boundary. |
| F004 | accepted | EPICS, ioc-runner, con, procServ, conserver, and NFS simulation are validation of the Linux baseline, not the repository's primary identity. | C3, R2-F4 | DM5 and DM6 remain after baseline and overlay boundary decisions. |
| F005 | accepted | Makefile target generation needs a per-playbook topology model, especially for out-of-band or server-only validation stages such as `04_nfs_sim`. | R3-F2 | DM7 must define topology, not only clean help text. |
| F006 | accepted | Local and NFS-backed ioc-runner source-root validation requires apply-level verification, not syntax-only closure. | R3-F3 | DM6 closes only after real server-host apply and post-apply checks. |
| F007 | accepted | The implementation plan must order gates before edits so purpose text, VM ownership, topology, and validation do not drift independently. | R3-F4 | DM9 becomes a staged development plan with gate IDs and verification IDs. |
| F008 | accepted | NFS simulation paths currently leak the `alsu` namespace, and `vmadmin` is a testbed identity that must be classified or parameterized for a public baseline. | External review, verified by `rg -n "alsu|vmadmin" README.md docs inventory roles playbooks configure` | DM4/P3 must explicitly classify or remove `alsu` and `vmadmin` assumptions; verification must search for these identifiers. |

## Rejected Findings

No reviewer findings are rejected.

## Deferred Findings

| ID | State | Finding | Reason |
| --- | --- | --- | --- |
| D001 | deferred | Add a minimal public `virt/` layer to this repository. | Reviewers agree this remains plausible, but first reorientation should keep `cloud-provision` as the single VM lifecycle source of truth. |

## Open User Decisions

No implementation-scoped reviewer finding remains blocked. The main owner
decision is recorded as settled for the first implementation pass: keep VM
lifecycle delegated to `cloud-provision`.

## Converged Design Milestones

| Gate | Milestone | Deliverable | Closure Verification |
| --- | --- | --- | --- |
| G1 | Repository purpose and boundary | Rewrite `README.md`, `docs/ARCHITECTURE.md`, and `docs/MILESTONES.md` so the repository is a public Linux system unification baseline with EPICS validation. | Docs state public baseline, site overlay boundary, supported OS set, and non-goals. |
| G2 | VM lifecycle source of truth | Keep VM lifecycle delegated to `cloud-provision`; treat `inventory/testbed.ini` as an example validation inventory. | No document presents an in-repository VM lifecycle as active source of truth. |
| G3 | Variable and overlay classification | Classify variables into baseline defaults, validation defaults, testbed defaults, and site-overlay-required values, including NFS path namespace and default testbed user identities. | `inventory/group_vars/all.yml`, `roles/nfs_sim/defaults/main.yml`, and docs make each variable class understandable to an external user; no unclassified `alsu` or `vmadmin` assumption remains. |
| G4 | Baseline architecture map | Map baseline roles and playbooks to Linux unification functions: OS packages, time sync, shell tools, firewall, runtime, and Python policy. | Architecture doc separates baseline duties from validation duties. |
| G5 | EPICS validation layer | Keep con, procServ, conserver, EPICS, ioc-runner, and NFS simulation as validation layers above the Linux baseline. | Milestones and architecture do not describe EPICS as the top-level repository purpose. |
| G6 | Local and NFS ioc-runner validation | Preserve local `03_epics` and NFS-backed `04_nfs_sim` coverage through source-root variables and forced NFS validation. | Syntax checks pass; testbed apply proves `ioc-runner -V` and `ioc-runner inspect` for local and NFS paths. |
| G7 | Makefile topology model | Align generated targets with playbook host scope so unsupported node targets are not implied. | `make help.detail` and dry-run commands reflect all-node, OS-group, server-only, and out-of-band stages correctly. |
| G8 | Stale artifact and documentation cleanup | Remove unused artifacts and rewrite docs toward architecture, functional specifications, and data flow. | `rg` shows no stale template references; docs avoid troubleshooting history. |

## Implementation Boundary

Implementation should be documentation-first, then topology and validation.
Do not add a `virt/` layer in the first pass. Do not import ALS-U internal
values from `ansible-alsu-linux-os`. Remove or parameterize public-baseline
leaks such as `alsu` path namespaces and classify `vmadmin` as a testbed
default rather than a public baseline identity.

## Promotion Required

No ADR is required yet. If future implementation adds an in-repository VM
lifecycle, that decision should be promoted to a durable architecture decision.
